import type { IconType } from "react-icons";

export interface footerNav {
    id: string;
    name: string;
    url: string;
}

export interface footerContact {
    id: string;
    icon: IconType;
    url: string;
}